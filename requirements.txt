# Central Audit - Sistema Web
# Dependências necessárias para funcionamento completo

# Framework Web
Flask==2.3.3
Flask-SocketIO==5.3.6

# Automação Web
selenium==4.15.2
webdriver-manager==4.0.1

# Interface Gráfica (mantida para compatibilidade)
PySimpleGUI>=4.60.5

# Manipulação de Dados
pandas>=2.0.0
openpyxl>=3.1.0

# Requisições HTTP
requests==2.31.0

# Manipulação de JSON (já incluído no Python)
# json - biblioteca padrão

# Data e Hora (já incluído no Python)
# datetime - biblioteca padrão

# Sistema Operacional (já incluído no Python)
# os - biblioteca padrão

# Threading (já incluído no Python)
# threading - biblioteca padrão

# Entrada/Saída (já incluído no Python)
# io - biblioteca padrão

# Subprocess (já incluído no Python)
# subprocess - biblioteca padrão

# Pathlib (já incluído no Python)
# pathlib - biblioteca padrão

# Novas dependências adicionadas
python-socketio==5.9.0
python-engineio==4.7.1
Pillow==10.0.1
python-dotenv==1.0.0
gunicorn==21.2.0
eventlet==0.33.3 